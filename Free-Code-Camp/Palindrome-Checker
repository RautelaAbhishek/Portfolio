function palindrome(str) {
  var rm = /[\W_]/g;
  var lower = str.toLowerCase().replace(rm,'');
  var reverse = lower.split('').reverse().join('');
  return reverse === lower;
}



palindrome("eye");
